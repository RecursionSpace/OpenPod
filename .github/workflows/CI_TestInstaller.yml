name: CI | Installer

on: [push]

jobs:
  install:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Installer
        run: |
          sudo /bin/bash installer.sh
